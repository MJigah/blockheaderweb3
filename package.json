{
  "name": "blockheaderweb3",
  "private": true,
  "workspaces": [
    "section-a",
    "section-b"
  ],
  "scripts": {
    "dev:section-a": "npm --workspace section-a run dev",
    "dev:section-b": "npm --workspace section-b run dev",
    "build:section-a": "npm --workspace section-a run build",
    "build:section-b": "npm --workspace section-b run build"
  }
}
